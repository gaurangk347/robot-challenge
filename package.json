{
  "name": "robot-challenge",
  "version": "1.0.0",
  "description": "Robot challenge using TypeScript.",
  "author": "Gaurang Kathiriya",
  "license": "ISC",
  "scripts": {
    "build": "echo Using TypeScript && tsc --version && tsc --pretty",
    "start": "npm run build && node ./lib/index",
    "test": "npm run build && jest"
  },
  "main": "lib/index.js",
  "bin": {
    "toy-robot": "./lib/index.js"
  },
  "dependencies": {
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "@types/jest": "^28.1.3",
    "@types/lodash": "^4.14.182",
    "@types/node": "^18.0.0",
    "jest": "^28.1.1",
    "ts-jest": "^28.0.5",
    "ts-node": "^10.8.1",
    "typescript": "^4.7.4"
  }
}
